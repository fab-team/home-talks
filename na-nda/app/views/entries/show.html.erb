<% @page_title = @entry.title + " - " + @entry.author.name + "さんからのクイズ" %>
<% @page_description = @entry.title + " - " + @entry.author.name + "さんからのクイズ" %>
<h1><%= @entry.author.name %>さんからのクイズ</h1>
<h2><%= @entry.title %></h2>

<% if current_member == @entry.author %>
  <p><%= link_to "編集", [:edit, @entry] %></p>
<% end %>

<%= simple_format(@entry.body) %>
<%= simple_format(@entry.hint) %>
<%= simple_format(@entry.answer) %>

<%= render "footer", entry: @entry %>

<%= render "votes" %>
